export type GeoLocationType = {
  name: string;
  country: string;
  currency: string;
}
export type FeaturesType = {
  city?: string,
  country?: string,
  countrycode?: string,
  county?: string,
  district?: string,
  state?: string,
  housenumber?: string,
  name?: string,
  postcode?: string,
  street?: string,
  type?: string,
  coordinates?: number[],
  selected?: boolean,
}
